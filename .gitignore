# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.venv/
venv/

# SQLite 数据库（本地运行时数据，不入库）
*.db
*.sqlite
*.sqlite3

# 归档快照 / 构建产物
*.zip

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# Local-only files (working scripts, backups, AI memory)
.gitignored/

# === Dev tooling artifacts (added for Makefile + CI) ===
# ruff / mypy / coverage
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
coverage.xml

# build / dist (sdist + wheel)
build/
dist/
*.egg-info/

# pip-audit
.pip-audit/

# 部署脚本在服务器上生成的备份快照（不进 git）
.backups/

